       <div class="bg-header">
        <div class="flat-header-blog">
            <div class="top-bar clearfix">
                <div class="container">
                    <div class="row">
                        <div class="col-lg-6 col-md-8 col-sm-12 col-xs-12">
                            <ul class="information">
                                <li class="phone lt-sp003">
                                    <i class="fa fa-phone" aria-hidden="true"></i>  +213 552 448 812
                                </li>
                                <li class="email">
                                    <i class="fa fa-envelope" aria-hidden="true"></i> ecolepsc@info.com
                                </li>
                            </ul>
                        </div>
                        <div class="col-lg-6 col-md-4 col-sm-12 col-xs-12">
                    {% if is_granted('ROLE_USER') == false %}
                            <ul class="nav-sing">
                                <li><a href="{{ path('fos_user_security_login') }}">{{ 'Menu.log_in'|trans}}</a></li>
                                <li><a href="{{ path('fos_user_registration_register') }}">{{ 'Menu.Registerp'|trans}}</a></li>
                            </ul>
                    {% else %}
                            <ul class="nav-sing">
                                <li><a href="{{ path('dashboardcours') }}">{{ 'Menu.Registerm'|trans}}</a></li>
                                <li><a href="{{ path('fos_user_security_logout') }}">{{ 'Menu.Disconnect'|trans}}</a></li>
                            </ul>
                    {% endif %}
                        </div>
                    </div>
                </div>
            </div>
            <header class="header menu-bar header-blog hv-menu-type2">
                <div class="container"> 
                    <div class="menu-bar-wrap clearfix">
                        <div id="logo" class="logo">
                            <a href="{{path('home')}}"><img src="{{asset('asset/img/images/logo/07.png')}}" alt="images"></a>
                        </div>
                        <div class="mobile-button"><span></span></div>
                        <div class="header-menu">
                            <nav id="main-nav" class="main-nav">
                                <ul class="menu">
                                    <li><a href="{{path('home')}}">{{ 'Menu.Profile1'|trans}}</a></li>
                                    <li><a href="{{path('cours')}}">{{ 'Menu.Edit_Profile1'|trans}}</a></li>
                                    <li><a href="{{path('about')}}">{{ 'Menu.Change_password1'|trans}}</a></li>
                                    <li><a href="{{path('blog')}}"> {{ 'Menu.website'|trans}}</a></li>
                                    {% if is_granted('ROLE_USER') == false %}
                                    <li><a href="{{path('contact')}}">{{ 'Menu.personal_agenda'|trans}}</a></li>
                                    {% endif %}
                                </ul>
                            </nav>
                        </div>
                    </div>
                </div>
            </header>
            <div class="page-title page-title-blog">
                <div class="page-title-inner">
                    <div class="breadcrumbs breadcrumbs-blog text-left">
                        <div class="container">  
                            <div class="breadcrumbs-wrap">
                                <ul class="breadcrumbs-inner">

                                </ul>
                                <div class="title">
                                    {{controller_title}}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div><!-- bg-header -->